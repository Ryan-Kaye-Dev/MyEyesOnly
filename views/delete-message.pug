extends layout

block content
    div.form
        form(method="POST")
            h1.heading Confirm Delete
            input(type='hidden', name='messageid', value=message._id)
            p Are you sure you want to delete this message?
            button(type="submit") Yes, Delete
